# NCE PYMS 第一阶段核心任务审计与收口报告

- 日期：2026-09-04
- 当前基线：`NCE_PYMS_Functional_Roadmap_v1.0_2026-09-03.md`
- 审计范围：正式 CP/FT、个人分析工具、Cleaner 模块化、分析规则、图表结果、数据源与本地运行

## 一、结论

第一阶段的“人工可用主链路”已经形成：

1. 正式 CP/FT 可以上传源文件或提交受控服务器目录，后台调用已发布 Cleaner，形成正式 Dataset，并进入产品、批次和图表分析。
2. 个人分析工具可以直接读取同机、共享路径或个人电脑 Agent 可见的数据，执行一次性清洗、散点/图表、PAT 和 SBL/SYL，只在服务器保存结果和历史，不保留原始文件与清洗中间文件。
3. 本地 API、Worker、网页和 SQL Server 版本保护可以一键启动并同时确认就绪。

第一阶段尚不能宣告“全部完成”。当前剩余的核心缺口是：

- Cleaner 格式方法级 Registry 只完成了立昂微和电基，其余已接入厂家仍通过旧 Adapter 运行；
- 分析规则自动匹配的程序链路已完成，但当前开发库没有任何已批准并启用的分析规则，高级统计/质量图表会按设计保持不可用；
- 数据源中心已具备已配置目录查看、目录清单确认和人工提交入口；FTP/NAS 定时自动采集按 2026-09-04 决策暂缓，不再作为第一阶段阻塞项；
- 个人工具的清洗、散点/图表、PAT 和 SBL/SYL 已形成后台任务、结果历史和下载闭环；各厂家只显示其原桌面工具实际具备的能力；
- 当前结论是本地开发环境验收，不是目标服务器、真实登录权限、UAT 或生产上线结论。

因此，当前不继续横向增加菜单，也不展开 FTP/NAS 和 SAP-B1；后续按“现有功能稳定 → 重点厂家模块化 → 第一阶段业务验收”顺序收口。

## 二、历史规划完成度

### P0：产品边界与可用性收口

| 规划项 | 当前状态 | 已有证据 | 仍缺什么 |
|---|---|---|---|
| P0-1 正式数据与个人工具导航分层 | 已完成 | 菜单已分为 CP数据、FT数据、个人分析工具、产品与批次、良率与质量；本地页面实测可见 | 目标服务器和普通用户权限验收 |
| P0-2 个人工具注册与 CP/FT/VDMOS 分类 | 核心完成 | 4 个 CP、5 个 FT 厂家入口已按实际能力提供清洗、图表/散点、PAT、SBL/SYL，具备服务器历史和结果下载 | 其余厂家 Agent Golden；格式方法级 Registry；VDMOS 后续独立收口 |
| P0-3 正式规则自动匹配 | 程序完成、业务配置未完成 | 服务端只返回已批准、启用、生效且适用于当前数据集/参数的规则；普通页面不要求填写规则编号；本轮修复选择参数后重新匹配 | 当前开发库规则清单为空，需要基于批准口径登记规则，不能由程序猜测 |
| P0-4 通用分析结果合同 | 部分完成 | 正式分析已有统一 Context、描述统计、散点、趋势、明细、空间、质量和报告结果组件 | 个人 PAT 结果与正式分析结果仍是不同展示合同；高级方法受规则配置门禁 |
| 产品、Lot、厂家、来源术语统一 | 基本完成 | 当前菜单和核心页面已改为业务中文，删减过多解释性文字 | SAP 订单分类和未识别产品/Lot 的业务补录仍需收口 |

### P1：YMS 核心闭环

| 规划项 | 当前状态 | 说明 |
|---|---|---|
| FTP/NAS 自动采集闭环 | 暂缓 | 已完成受控目录浏览、清单指纹和人工提交；定时扫描、自动入队、失败重试和任务监控按 2026-09-04 决策移出当前阶段 |
| 产品与批次页面 | 基础完成 | 已有 Current Dataset、产品/Lot/阶段筛选、详情和分析入口；尚未形成产品 → Lot → Wafer/Unit 的完整业务钻取体验 |
| 产品良率驾驶舱 | 基础完成 | 已有近 30 天正式数据、已知良率、未知占比和最近数据；尚未形成异常 Lot/参数的管理闭环 |
| 跨批次、跨厂家比较 | 部分完成 | 支持同阶段多 Dataset 分析并阻止 CP/FT 混选；数据兼容性原因展示仍需完善 |
| 图表组合与综合报告 | 部分完成 | 已有保存分析、HTML/PDF/XLSX 导出基础；模板化组合和业务验收未完成 |
| 异常与任务闭环 | 未完成 | 当前有 Job 和失败详情，没有异常发现、确认、分派、处理、关闭的完整业务流程 |

### P2 与 P3

SAP-B1 主数据和订单辅助已按 2026-09-04 决策暂缓。CP→封装→FT 谱系、WAT/WIP/可靠性、客诉、动态异常、根因候选和 AI 产品工程助手均未进入第一阶段完成范围，不应在当前主链路稳定前并行展开。

## 三、Cleaner 模块化现状

### 已完成

- 立昂微 CP：动态参数 Excel、已批准 OLE XLS 两种格式方法；正式 Release 59 已验证。
- 电基 FT：PowerTECH 文本伪 XLS、原生 XLSX；个人 PAT 额外包含 STS8203 CSV、TF CSV。
- Runner 会核对测试阶段、厂家、Cleaner、输入合同、输出合同、格式方法和正式包 SHA，错配时失败关闭。
- 历史 Dataset 不因 Cleaner 更新自动重算。

### 未完成

- 正式链路的华虹、积塔、日月新、日月光仍为旧 Adapter，没有拆成格式方法级能力。
- 个人快速 PAT 的华虹、积塔、国宇、日月新、日月光、杰群、集佳等尚未全部进入统一 Cleaner Capability Registry。
- 管理页面还不能直接查看“厂家 → 用途 → 格式方法 → 正式包版本 → SHA”的能力目录。

### 建议迁移顺序

1. 日月新 FT 正式清洗：现有正式 Dataset 数量多，先保护最常用链路。
2. 华虹 CP 正式清洗：现有 CP 数据与 Yield 证据完整，适合作为第二个 Golden。
3. 积塔 CP、日月光 FT：完成其余当前正式入口。
4. 杰群、集佳、国宇等个人工具：按个人 PAT 实际使用频率迁移，不反向扩大正式入库范围。

## 四、本轮核心开发

本轮先完成 P0-3，并追加收口个人工具已有能力，没有扩展新的业务算法。

已完成：

1. 服务端分析 Context 新增“当前上下文可用规则版本”，仅返回同时满足以下条件的规则：
   - 已发布；
   - 已启用且处于有效期；
   - 业务、技术、质量三方均批准；
   - 测试阶段、厂家、产品和参数范围匹配；
   - 对本次选择的全部 Dataset 均适用。
2. CP/FT 图表选择页根据服务端返回的适用规则决定高级方法是否可用，普通用户不再填写规则编号或版本。
3. 修复参数范围规则的闭环：用户选择参数后，页面会携带已选参数重新请求 Context；否则参数级规则会一直被误判为不可用。
4. 重新匹配期间保留已加载的参数列表，避免查询刷新把用户刚选择的参数清空。
5. 增加参数级规则重新匹配的前端回归测试。
6. 个人工具新增数据清洗、散点/图表和 SBL/SYL 后台任务；继续调用已发布 CP/FT 工具包，不在 TMS 重写厂家解析和计算逻辑。
7. 清洗和图表结果保存为 ZIP，SBL/SYL 保存为 XLSX；原始文件和清洗中间数据不进入正式数据库，任务结束后清理临时目录。
8. 历史列表增加功能类型与通用“下载结果”，PAT 明细仍保留原参数结果展开能力。
9. SQL 任务类型约束升级至 `sql2014_0026`，并为会话详情和结果下载增加 SQL Server 死锁重试。

## 五、验证结果

### 本地运行

- API：Ready。
- Worker：Ready。
- 前端：Ready。
- 数据库：`TMS_G0_DEV`。
- Schema：`sql2014_0026`。
- API 与 Worker 数据库、Schema、服务器身份一致。

### 真实环境只读核对

- CP 正式结果：16 条；可见华虹、积塔、立昂微和历史国宇结果。
- FT 正式结果：25 条；可见日月新、日月光和电基结果。
- 本轮新增真实任务 10 个：杰群 SBL/SYL、杰群散点、华虹清洗、日月新清洗/散点、日月光清洗、电基清洗/散点/SBL/SYL、集佳清洗，均成功并可下载结果。
- 真实个人历史中包含 520 个文件、约 2.83 GB、6,813,800 行的杰群 PAT 结果。
- 当前分析规则清单为空；高级规则型分析保持关闭，未使用默认值或猜测值绕过审批。

### 自动化

- 后端全部单元测试：1,171 passed，4 skipped。
- 个人工具、数据库迁移、任务服务和 Worker 定向测试：72 passed。
- Cleaner 模块化与规则匹配核心测试：11 passed（包含在上述相关验证的交叉集合中）。
- CP/FT 页面、导航、数据源中心和图表选择：44 passed。
- 本轮所有修改过的前端测试批量执行：282 passed，5 个在四并发运行时超过原超时；5 个均在单文件或串行复验通过。
- 参数分析、参数关系和分析导出复验：22 passed。
- 产品与批次复验：17 passed。
- 前端 TypeScript 检查和生产构建：通过。
- 个人工具前端定向测试：15 passed；全套并发测试中的超时/合同失败文件经单进程复验后 72 passed。
- `git diff --check`：通过。

## 六、确定的边界

- 第一阶段可用不等于所有高级质量规则已经批准；FTP/NAS 无人值守采集与 SAP-B1 关联已明确暂缓。
- 没有 PASS/FAIL、Bin 或正式规格时，Yield、OOS、Cpk/Ppk 等保持未知或不适用，不能补 0。
- 个人分析结果不得进入正式良率、管理驾驶舱和异常预警。
- 当前旧 Adapter 可以继续提供已经验收的能力；迁移 Registry 时不得重写成熟厂家解析器。
- 国宇目前属于个人定制工具范围，不能因为历史正式数据存在就自动重新开放正式通用上传。

## 七、下一步执行顺序

1. **稳定个人工具现有能力**：补齐各厂家代表样本 Golden、失败提示和普通用户权限验收，不增加原工具没有的功能。
2. **完成第一阶段规则配置门禁**：盘点 CP/FT 现有正式算法和批准口径；没有业务批准的规则继续不可用。
3. **迁移日月新 FT Cleaner 能力**：拆分 DC 等已验收格式方法，登记正式包 SHA 和合同，用真实源数据做 Golden 对账。
4. **迁移华虹 CP Cleaner 能力**：保持现有正式上传和标准结果不变，只替换能力登记与调度约束。
5. **做第一阶段业务验收**：至少使用一个 CP 产品和一个 FT 产品，从上传/目录提交走到清洗结果、基础图表和历史查询；确认个人任务只保存结果。

FTP/NAS 定时采集和 SAP-B1 关联等用户重新排期后再启动，不影响当前第一阶段可用性收口。
