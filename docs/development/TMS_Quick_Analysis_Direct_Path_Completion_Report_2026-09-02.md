# TMS 快速分析直连目录重构完成报告（2026-09-02）

## 做了什么

- 快速分析前台取消 Local Agent、8765 端口、配对 Token 和独立工具安装等用户操作。
- 新增“本机 / NAS 路径”：输入当前 TMS 主机可访问的本地盘符、映射盘符或 UNC 目录，先递归预览 CSV 范围，再创建个人 PAT 任务。
- Worker 对个人直连任务直接读取源目录，并调用 Cleaner Registry 中已发布的杰群 FT PAT 包；没有复制或重写 PAT 算法。
- 预览、排队前和计算完成前都使用同一文件数、大小、修改时间 Manifest。范围变化时停止任务并要求重新预览。
- 保留“已配置服务器”作为共享、定时和反复使用的 FTP/NAS 主入口，结果权限继承数据域。
- 新增“临时 FTP 预览”，支持 FTP/显式 FTPS 地址、端口、账号、密码和远程目录；密码只用于本次请求，不写入配置或响应。
- NAS 已由直连目录支持当前 Windows 账号可访问的映射盘符或 UNC；需要独立账号的 NAS 连接仍由后台配置承担。
- CP 快速 PAT 工具入口保留为禁用项。当前 CP 工具尚没有可从 TMS 调用的原始目录 PAT 合同，因此未伪造完成状态。

## 确定的权限行为

- 本机/UNC 直连任务是 `PERSONAL`，默认只有发起人以及开发期全数据管理员可见。
- 已配置服务器任务是 `DOMAIN`，依赖当前有效数据域授权。
- 临时 FTP 当前只返回目录预览，不生成共享数据权限；需要计算时配置为服务器数据源。
- 工程/量产和工厂不是本功能的数据权限条件。

## 已验证

- 后端快速分析、Source Catalog、结果接收与 Worker 相关回归：57 passed，1 skipped（环境条件跳过）。
- 前端快速分析与 API 合同测试：7 passed。
- 前端生产构建：通过。
- 代码风格检查（本轮修改文件）：通过。
- 开发环境 API、Worker、前端均已重启并处于 ready。
- 真实目录 `F:\data\CP和FT源数据\FT数据\杰群data\520data\NCEAP020N10LL`：识别 520 个 CSV、3,041,085,645 bytes；清单预览约 0.055 秒，未上传源文件。

## 尚未完成或限制

- 本轮没有启动真实 3.04 GB PAT 全量计算，以免完成验证时产生长时间任务；运行链已经由 Worker 单元/集成测试覆盖。
- 临时 FTP 只用于一次性目录确认，不把明文密码写入队列，因此不直接创建后台 PAT；高频场景需后台配置数据源。
- 部分 FTP 服务器不支持 MLSD 时，临时预览会提示改用后台配置。
- CP 原始目录 PAT Adapter、输出合同和真实样本回归尚未完成。
- 当前开发环境后端与数据目录在同一台电脑。正式集中部署后，用户电脑的 `F:\` 不会自动成为服务器可访问路径；应使用 NAS/FTP 或在用户电脑运行完整 TMS 单机版。

## 下一步

1. 从 CP 工具确认可稳定调用的原始目录 PAT 入口、参数和输出合同，再接入同一个直连页面。
2. 用实际 FTP 服务器验证临时预览，并把常用 FTP 根目录登记到后台 Source Catalog。
3. 核心功能稳定后，按安全延期清单统一增加路径白名单、凭据托管和上线前安全测试。
