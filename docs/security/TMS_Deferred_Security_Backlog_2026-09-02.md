# TMS 安全延期清单（2026-09-02）

状态：`DEFERRED`。本清单不阻塞当前 CP、FT、FTP 和个人工具功能开发。

## 上线前统一处理

1. 管理员最小权限拆分，以及全数据访问是否改为按需启用。
2. break-glass 的申请、原因、时效、审批和审计闭环。
3. 默认账号、开发密码、历史 Session 和测试数据清理。
4. 密码策略、登录锁定、多因素认证和账号生命周期。
5. Local Agent 注册、设备绑定、升级签名、Token 轮换和吊销。
6. FTP/SAP/数据库服务账号的专用凭据保管和轮换。
7. 文件下载、导出、日志脱敏、审计留存和异常访问告警。
8. 路径穿越、软链接、压缩包、恶意文件和大文件拒绝服务测试。
9. API 速率、请求体限制、跨域、Cookie/Token 和网络暴露策略。
10. 上线前执行一次独立安全测试，并形成问题清单和关闭记录。

## 当前约束

- 不新增安全专项验收门槛。
- 不删除业务审计记录、数据血缘和原文件只读保护等基础可追溯能力。
- 功能测试继续覆盖登录、角色可用、数据查询、CP/FT 处理和 Local Agent 主路径。
